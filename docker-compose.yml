version: '3.8'

services:
  code-review-agent:
    build: .
    environment:
      - NVIDIA_API_KEY=${NVIDIA_API_KEY}
    volumes:
      - ./diffs:/app/diffs
      - ./results:/app/results
    working_dir: /app
