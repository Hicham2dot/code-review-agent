package server

// Chi's built-in middleware (middleware.Logger, middleware.Recoverer, middleware.RequestID)
// is wired in server.go:buildRouter(). This file is reserved for custom middleware if needed.
